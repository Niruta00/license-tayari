import 'package:flutter/material.dart';
class TrafficSymbols extends StatelessWidget {
  const TrafficSymbols({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.black,
      ),
    );
  }
}